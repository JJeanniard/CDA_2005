name: sync branch

on:
  push:
    branches: [ develop ]

jobs:
  build:
    name: merge
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - run: |
        git switch master
        git merger develop
